{
  "name": "poketokenbar-trade-server",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run src/index.test.ts",
    "check": "npm run typecheck && npm test",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.3",
    "typescript": "5.8.3",
    "vitest": "4.1.0",
    "wrangler": "4.128.0"
  }
}
